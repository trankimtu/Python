while (fahr < upper) a = 23.00 whileend
==
==
==
==
==
==

! Find largest value between two numbers!
int num1, num2, large$
if(num1 > num2)
{
	large = num1$;
}
else
{
	large = num2$;
}

==
==
==
==
==
==

1a
a+1b

(1a) + 2b(c+d)
e+2d
int float bool true false if else then endif whileend

test failure
end test failure

a_bc
a1bc_


aaabbb ccc 'ddd'
!!
zzz
!12345!

67890

! 
aaa
bbb
ccc
!
1 + 3 * 2
123+456-789/1000
(((9*10+5)/3 *2 ) * 2.5*length - width)-sum(3,2,1)
if a==bcd
    b=567
    a>=bcd
    c<=ddd
Function convert1x(fahr int)
{
    Return 5*(fahr-32)/9;
}

Get(low,high,step);

While (low<= high)

Int low,high,step; [*declaration*]
{
    Put(low);
    Put (convert1x(low));
    Low = low + step;
}

%%
