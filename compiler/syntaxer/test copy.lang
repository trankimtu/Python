int a;
a = 1;
a = b;
int b = 2

while (fahr < upper) a = 23.00 whileend;


aaabbb ccc 'ddd';
!!
zzz
!12345!

67890;

! 
aaa
bbb
ccc
!
1 + 3 * 2;
123+456-789/1000;
(((9*10+5)/3 *2 ) * 2.5*length - width)-sum(3,2,1);
if a==bcd
    b=567
    a>=bcd
    c<=ddd
Function convert1x(fahr int)
{
    Return 5*(fahr-32)/9;
}

Get(low,high,step);

While (low<= high)

Int low,high,step; [*declaration*]
{
    Put(low);
    Put (convert1x(low));
    Low = low + step;
}

%%
